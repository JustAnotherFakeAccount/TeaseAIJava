# Normalize line endings to LF on commit, checkout as LF
* text=auto eol=lf

# Binaries — no line ending conversion
*.jar binary
*.zip binary
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.class binary
